# 假面骑事 · 用户反馈汇总预设

> 来源：2026-08-18 至 2026-08-19 当前功能迭代中的用户直接反馈。  
> 处理：仅保留产品诉求，不包含私人资料。

## 已确认诉求

| 优先级 | 反馈 | 已采取动作 | 当前状态 |
|---|---|---|---|
| P0 | 功能门槛不能依赖复杂 Skill 口令，最好可以网页直接体验 | 建立公开演示站和 localhost 工作台 | 已完成 |
| P0 | 工作台中心需要明确的 Persona Driver，而不是普通卡片画廊 | 使用 Three.js 程序化变身器 | 已完成 |
| P0 | 启动必须保留清晰按钮，机械把手不能替代按钮 | 锁定态同时保留启动按钮与把手 | 已完成 |
| P0 | 点击启动后才播放音效，不能自动连续播放 | 音频序列绑定启动动作 | 已完成 |
| P0 | 角色名、英文指令、音效必须按顺序播放 | 使用“角色名 → 指令 → 随机音效”序列 | 本机已实现 |
| P0 | 启动后要唤起 Navi，把 Skill、指令、输入和输出变成对话 | 新增 localhost Persona Navi Bridge | 已创建真实对话 |
| P0 | 不要把假的文件名交给 Navi 后让它全盘搜索 | 改为服务端真实预设绝对路径 | 本轮修复 |
| P1 | 页面需要可以给别人体验 | 公开站保持模拟数据，不访问访客本机 | 已完成 |

## 仍需验证

- YouNavi 当前模型为什么返回 HTTP 400；Bridge 已正确显示原始错误，但未修改用户模型设置。
- 角色输出的口音和音色是否达到预期；当前播报与原始候选音效仍是两套来源。
- 从 YouNavi 侧边栏选择真实文件时，授权预览是否足够清楚。
- 对话创建耗时较长时，“正在启动 YouNavi / 正在创建”是否需要更细阶段反馈。

## 明确反模式

- 只显示没有样式和文案的箭头，让用户猜测如何启动。
- 把“任务已入队”写成“任务已完成”。
- 只有文件名没有路径，却让 Agent 扩大到整个用户目录查找。
- 用模拟输出掩盖真实 task 的失败状态。
- 自动把模型输出覆盖到人物卡的事实字段。
